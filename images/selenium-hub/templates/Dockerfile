FROM        selenium/hub:3.0.1
MAINTAINER  ${DOCKER_MAINTAINER}

LABEL       ${DOCKER_LABEL_GROUP}
LABEL       ${DOCKER_SELENIUM_HUB_LABEL_APP_GROUP}
LABEL       ${DOCKER_SELENIUM_HUB_LABEL_APP_NAME}
